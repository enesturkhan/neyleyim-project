import type { SiteMetadata } from "@/types";

import { mainNavigation } from "@/components/navigation/navigation.config";

export const siteMetadata: SiteMetadata = {
  title: "Neyleyim",
  description:
    "Neyleyim — nefesin izinde bir tasavvuf musikisi topluluğu. Ney, kudüm ve bendir ile gönle açılan bir yolculuk.",
  locale: "tr",
};

export const primaryNav = mainNavigation;
